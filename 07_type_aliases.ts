const User1 = {
    name: "Ashutosh",
    email: "lord@puneet.com",
    age: 69,
    isValid: true
}

type User = {
    name: string,
    email: string,
    age: number,
    isValid: boolean
}

function createUser(user: User) {
    
}

createUser(User1);